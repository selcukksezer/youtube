/** Legacy cocuk animasyon prompt yardimcilari — sarki-only uygulamada stub. */
export function inferKidsLocationMedium(_text: string): string {
  return "UNKNOWN";
}

export function kidsMediumPhysicsLock(_medium: string): string {
  return "";
}

export const KIDS_FIXED_FILM_CRAFT_LOCK = "";

export function buildStoryWordVisualLock(_dialogue: string, _scene: string, _clipSeconds: number): string {
  return "";
}
