# P3-29: optional one-service smoke stack
services:
  shorts-api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./output:/app/output
      - ./assets:/app/assets
      - ./bgm:/app/bgm
    restart: unless-stopped
