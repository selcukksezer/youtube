# YouTube Shorts Ultimate — Environment Configuration
# Copy to .env OR run: python scripts/env_vault.py unseal

# AI Model Provider Keys
# GEMINI: Developer API key (aistudio.google.com). Google AI Plus chat/görsel kotası buraya geçmez.
# Senaryo = ücretsiz API tier yeterli. Görsel (Nano Banana) = billing gerekir; varsayılan kapalı.
GEMINI_API_KEY=
OPENAI_API_KEY=
DEEPSEEK_API_KEY=
GROK_API_KEY=
FAL_API_KEY=
# Alias also accepted: FAL_KEY=
STABILITY_API_KEY=

# ── Free / freemium AI text-to-video (RESEARCH_FREE_AI_VIDEO_APIS.md) ──
# Mixer treats AI + stock + procedural as equal peers (not last-resort).
# USE_AI_VIDEO: empty = auto-on when any key below is set; true/false to force.
USE_AI_VIDEO=
VISUAL_MIX_AI=0.4
VISUAL_MIX_STOCK=0.4
VISUAL_MIX_PROCEDURAL=0.2
# Optional fixed seed for reproducible per-scene mix (empty = job-based)
VISUAL_MIX_SEED=
# Fal.ai Wan/Hunyuan — signup credits then PAYG (best free-quality DX)
# HF Inference Providers — $0.10/mo free credit
HF_TOKEN=
# Replicate — limited free tries then PAYG
REPLICATE_API_TOKEN=
# DeepInfra Wan — PAYG (optional trial)
DEEPINFRA_TOKEN=
# PiAPI Kling gateway — ~$0.50 signup credits
PIAPI_KEY=
# Self-host / ComfyUI proxy: POST {url}/generate → {url|path}
LOCAL_AI_VIDEO_URL=
# Optional paid MiniMax / Hailuo (free tier is non-commercial — do not rely on it)
MINIMAX_API_KEY=
MINIMAX_GROUP_ID=
# Runway Gen-4.5 paid API (commercial) — https://dev.runwayml.com
RUNWAYML_API_SECRET=
# Luma Ray paid API (commercial) — free web tier is NOT commercial
LUMA_API_KEY=
# OpenAI Sora Videos API (deprecated 2026-09-24) — set USE_OPENAI_SORA=false to disable
USE_OPENAI_SORA=true
# Kids animation Shorts — see RESEARCH_KIDS_ANIMATION_AI.md
# AI_VIDEO_STYLE_PRESET=kids_cartoon  # auto for niche 36_kids_animation
AI_VIDEO_CLIP_MAX_SEC=5

# ── Higgsfield Cloud (https://console.higgsfield.ai) — PAYG, commercial OK ──
# Auth = Key ID + Secret. Do NOT put HuggingFace HF_TOKEN here.
# Form A (recommended):
HIGGSFIELD_CREDENTIALS=
# Form B (official doc names also work as HF_API_KEY_ID / HF_API_KEY_SECRET):
HIGGSFIELD_API_KEY_ID=
HIGGSFIELD_API_KEY_SECRET=
# Default T2V model (override from console catalog):
HIGGSFIELD_T2V_MODEL=bytedance/seedance-2.5/text-to-video
HIGGSFIELD_BASE_URL=https://api.higgsfield.ai
HIGGSFIELD_RESOLUTION=720p
# Keep false — our Edge/Azure TTS owns narration audio
HIGGSFIELD_GENERATE_AUDIO=false

# Video Source API Keys
PEXELS_API_KEY=
PIXABAY_API_KEY=
YOUTUBE_DATA_API_KEY=

# Reddit OAuth (optional — for live Reddit discovery)
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USER_AGENT=YouTubeShortsStudio/1.0 (research workflow)

# ElevenLabs free tier — https://elevenlabs.io/
# Settings UI writes here; if you use .env.vault run: python scripts/env_vault.py seal
ELEVENLABS_API_KEY=
ELEVENLABS_MODEL_ID=eleven_multilingual_v2

# Application Settings
LANGUAGE=tr
TTS_GENDER=male
TTS_VOICE=
TTS_RATE=+18%
ENABLE_BGM=true
BGM_VOLUME=0.12
DEFAULT_BGM_TRACK=
TTS_PITCH=+0Hz
SUBTITLE_FONT_SIZE=54
SUBTITLE_COLOR=white
SUBTITLE_HIGHLIGHT_COLOR=#FFD700
SUBTITLE_Y_POSITION=0.8
ENABLE_SFX=true
SFX_VOLUME=0.20
GAMEPLAY_ROTATION_WINDOW=75
ENABLE_DIRECTOR_PLAN=true
ENABLE_FFMPEG_GRAPH=true
GEMINI_MODEL=gemini-flash-lite-latest
GEMINI_IMAGE_MODEL=gemini-2.5-flash-image
GEMINI_VIDEO_MODEL=veo-3.1-fast-generate-preview
GEMINI_TTS_MODEL=gemini-2.5-flash-preview-tts
# Opsiyonel — açmadan stok video (Pexels/Pixabay) kullanılır; Plus abonelik API faturasını karşılamaz
USE_GEMINI_IMAGE_GEN=false
USE_GEMINI_VIDEO_GEN=false
GEMINI_VEO_PAID_QUOTA=false
GEMINI_VEO_PREVIEW_ONLY=true
USE_GEMINI_TTS=false
USE_GEMINI_GROUNDING=false
USE_GEMINI_EMBEDDINGS=false
PREFER_GEMINI_SCENE_IMAGES=false
AUTO_FETCH_ROYALTY_FREE_BGM=true
WHISPER_ALIGN=false
# false = tüm overlay/efekt aktif (Batch 1+2 görünür). true = ağır efekt bypass (varsayılan eski davranış)
RENDER_SAFE_MODE=false
RENDER_RESOLUTION_MODE=1080p
RENDER_THREADS=8
# FFmpeg thread count (RENDER_THREADS defaults to this); FFMPEG_DEBUG=true prints full ffmpeg stderr
FFMPEG_THREADS=4
FFMPEG_DEBUG=false
USE_GPU_ACCELERATION=true
GPU_CODEC=h264_nvenc
FPS_DIVERSIFY=true
# Item 323: 30 (default) or 60 for smoother export
EXPORT_FPS_MODE=30
# Item 324: did | sadtalker | liveportrait (paid — leave empty to disable)
AVATAR_ANIMATION_PROVIDER=
AVATAR_ANIMATION_API_KEY=

# P2-24 Stock copyright blocklist (comma-separated asset IDs or contributors)
STOCK_BLOCKLIST_IDS=
STOCK_BLOCKLIST_CONTRIBUTORS=

# Ops — server port for run.py / run_ui.sh (default 8000)
PORT=8000
# BGM RAM cache size (tracks kept in memory during render)
BGM_RAM_CACHE_MAX=12
# Force procedural (no-AI) scenario fallback — debugging only
SHORTS_FORCE_PROCEDURAL_FALLBACK=false

# Item 439 — encrypted SQLite backup (data/backups/*.db.enc)
# Restore: python database.py restore data/backups/<file>.db.enc restored.db
ENABLE_DB_BACKUP=false
DB_BACKUP_INTERVAL_HOURS=24
# Falls back to ENV_VAULT_PASSPHRASE, then to a fixed local default (weak — set one!)
DB_BACKUP_PASSPHRASE=
# Docker only: keep DB on the persisted ./data volume (docker-compose sets this)
# SHORTS_DB_PATH=/app/data/shorts.db

# Render error notifications (optional; both empty = disabled)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
DISCORD_WEBHOOK_URL=
